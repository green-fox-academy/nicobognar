#include <iostream>

std::string changeString(std::string input);

int main() {
    std::cout << changeString("xalla") << std::endl;
    return 0;
}

std::string changeString(std::string input){
    int pos;
    if(){

    }
}


// Given a string, compute recursively (no loops) a new string where all the lowercase 'x' chars have been changed to 'y' chars.